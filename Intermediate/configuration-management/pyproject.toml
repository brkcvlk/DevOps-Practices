[project]
name = "configuration-management"
version = "0.1.0"
description = "Ansible Configuration Management Playbook with Roles"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=13.4.0",
]
